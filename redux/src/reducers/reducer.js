const initialState = {
    records: null,
    isLoading: false,
    error: '',
    test: 'ITS WORKING'
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {




    default:
        return state;
    }
}